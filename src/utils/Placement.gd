class Placement:
	var position
	var next_direction
	var previous_direction
	
	func _init(position, next_direction, previous_direction):
		position = position
		next_direction = next_direction
		previous_direction = previous_direction
